name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
    - name: Hello World
      run: echo "Hello World"

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
